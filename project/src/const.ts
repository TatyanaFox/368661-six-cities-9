export enum AppRoute {
  Login = '/login',
  FavoriteFlats = '/favorite-flats',
  PropertyPage = '/property-page',
  Root = '/',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
